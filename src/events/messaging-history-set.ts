import type { Chat, Contact, WAMessage } from "baileys";

export const handleMessagingHistorySet = async ({
	chats,
	contacts,
	messages,
}: {
	chats: Chat[];
	contacts: Contact[];
	messages: WAMessage[];
}) => {};
