import type { Chat, Contact, WAMessage } from "baileys";

export const MessagingHistorySet = async ({
  chats,
  contacts,
  messages,
}: {
  chats: Chat[];
  contacts: Contact[];
  messages: WAMessage[];
}) => {};
